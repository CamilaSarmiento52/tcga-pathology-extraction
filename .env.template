OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here
MLFLOW_TRACKING_URI=sqlite:///mlflow.db

# Local OpenAI-compatible provider (ollama:* model strings). Defaults to the
# standard ollama endpoint; override for a remote host.
OLLAMA_BASE_URL=http://localhost:11434/v1

# Any other OpenAI-compatible runner (vllm, LM Studio, …) — used for non
# openai:/ollama: model prefixes.
LLM_BASE_URL=
LLM_API_KEY=
